import CourseworkClient from "@/components/coursework/CourseworkClient";

export const metadata = { title: "Coursework — PathToCPA" };

export default function CourseworkPage() {
  return <CourseworkClient />;
}
