import AllocateClient from "@/components/allocate/AllocateClient";

export const metadata = { title: "Allocate Courses — PathToCPA" };

export default function AllocatePage() {
  return <AllocateClient />;
}
